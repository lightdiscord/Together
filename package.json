{
  "name": "together",
  "private": true,
  "devDependencies": {
    "lerna": "^3.4.3"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "build": "lerna run build",
    "netlify": "npm run bootstrap && npm run build"
  }
}
